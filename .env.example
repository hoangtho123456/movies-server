# Environment variables are loaded for Prisma via `import "dotenv/config"` in prisma.config.js.
# See https://pris.ly/d/connection-strings for all connection string options.

# Neon direct (unpooled) connection - used by both the Prisma CLI (migrate) and the app.
DATABASE_URL=""
NODE_ENV=""
JWT_SECRET=""
JWT_EXPIRES_IN=""
